const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["assets/MarkdownView-R7unlZE-.js","assets/index-BJTH-yyb.js","assets/index-Dn0Gaiwu.css"])))=>i.map(i=>d[i]);
import{r,j as n,C as t,G as i,_ as d}from"./index-BJTH-yyb.js";const c=r.lazy(()=>d(()=>import("./MarkdownView-R7unlZE-.js").then(e=>e.M),__vite__mapDeps([0,1,2])));function m({onClose:e}){const o=r.useRef(null);return r.useEffect(()=>{const a=s=>{s.key==="Escape"&&e()};return window.addEventListener("keydown",a),()=>window.removeEventListener("keydown",a)},[e]),n.jsx("div",{className:"modal-overlay",role:"dialog","aria-modal":"true","aria-label":"关于跨贴",onClick:a=>a.target===a.currentTarget&&e(),children:n.jsxs("div",{className:"card modal-card about-card",children:[n.jsxs("div",{className:"about-head",children:[n.jsx(t,{size:40}),n.jsxs("div",{className:"about-title",children:[n.jsx("strong",{children:"跨贴 Crosspaste"}),n.jsxs("span",{className:"muted",children:["v","0.1.6"]})]})]}),n.jsx("p",{className:"about-desc",children:"跨贴是一款运行于飞牛 NAS 的网页端剪贴板应用，可将手机、电脑、平板等多设备复制的文本、图片、视频、音频与文件集中收纳，通过浏览器随时存取。界面自适应 PC 与移动端：PC 端采用常驻侧栏与多列布局，移动端切换为抽屉导航与单列布局，多端共用同一份数据。支持文本类型自动识别、Markdown 排版渲染与图片缩放预览，并可调用设备摄像头拍照录像、麦克风录音后即时归档。内容管理支持分组、标签、收藏与全文检索，分组可设置独立访问密码；对外提供只读分享链接，并附访问与下载统计。文件类型图标按内容大类（文档 / 媒体 / 压缩与安装包 / 代码与配置）着色；设置页内置「关于」弹窗，可查看应用介绍与完整更新日志。通过飞牛桌面入口经统一网关访问，免记端口并复用系统登录态，同时保留独立端口供局域网直连。"}),n.jsx("h3",{className:"about-subtitle",children:"更新日志"}),n.jsx("div",{className:"about-body",children:n.jsx(r.Suspense,{fallback:n.jsx("div",{className:"muted",children:"加载中…"}),children:n.jsx(c,{source:`## [0.1.6] - 2026-09-24

### 新功能

- **「关于」弹窗**：设置页底部的版本号成了入口，点开显示应用介绍与完整更新日志。两段内容都不
  另存副本——应用介绍取自 \`crosspaste/manifest\` 的 \`desc\`，更新日志就是仓库的 \`CHANGELOG.md\`，
  都在构建时读入并注入，改哪边都是改源头。更新日志按 Markdown 渲染，渲染链路（marked /
  DOMPurify / highlight.js）连同整篇日志一起走懒加载的独立 chunk，不进首屏主包；日志的文件头
  （一级标题与那句"本文件怎么记录"的维护者说明）在注入时剥掉，只留版本条目。滚动日志时，当前
  版本的标题会吸附在顶部直到被下一版替换。弹窗底部的 GitHub 入口是悬停会重描线、摆尾巴的图标。

### 改进

- **文件类型图标按内容大类着色**：147 个扩展名图标此前用 \`currentColor\` 单色渲染，"这是什么
  类型的文件"只能靠读图标形状辨认。现在按文档 / 媒体 / 压缩与安装包 / 代码与配置四类分别取色，
  覆盖 75 个常见后缀，未归类的保持单色。三个强调色原本只是配浅底色块用的粉彩，直接拿去画 18px
  图标在白底上淡到看不见，因此另取了一档"前景档"取值（\`--mint\` 本身仍在标签底、焦点环、引用块
  左边框处当装饰色用，没有动它）。
- **待办进度条吸顶**：滚动长清单时完成度始终可见。三处落点各不相同——卡片预览自成滚动容器
  （坐标系从预览区算起）、详情页要避开 60px 的吸顶顶栏、分享页的内容块带 14px 内边距且底色是
  半透明主色（用同款叠法还原出不透明等效底色，并补一块伪元素盖住内边距那截）。
- **分组色板与主题同调**：原先 7 个色的饱和度在 39–91%、明度在 36–63% 之间参差，与主色
  \`#2d5b63\`（S≈37%）不在一个调性上；且 \`#F5A623\` 与 \`#E67E22\` 都是橙、只差 12°，10px 的小色块上
  分不出来。现在统一到 S=42% / L=48%，色相从主题的青绿绕到琥珀，并去掉重复项。

### 修复

- **浅色主题下待办勾选框显示为黑方块**：根因不在勾选框自身的样式，而在 \`index.html\` 的
  \`<meta name="color-scheme" content="dark light">\`——它写死了"首选深色"，浏览器据此把原生控件
  按暗色渲染；用户切到浅色主题时 \`data-theme\` 变了、CSS 变量变了，这个 meta 却不会跟着变，
  勾选框继续按暗色绘制，落在白色卡片上就是一块黑块（深色主题下碰巧一致，所以只在浅色下暴露）。
  现在 \`color-scheme\` 改由 CSS 按主题控制，顺带让滚动条等原生控件也跟着主题走。
- **勾选框整个画不出来**：给所有输入框统一外观的那条全局规则，把 \`padding: 10px 12px\` 也打到了
  checkbox 上。原生勾选控件会忽略 \`background\` / \`border\`，却不会忽略 \`padding\`，而勾选框的宽高
  被固定成 16px，\`border-box\` 下左右内边距就要占 24px，内容盒直接被挤成 0。受影响的还有
  Markdown 渲染出来的任务清单、以及恢复码弹窗里那个"我已保存"的勾选框。另外，勾选框未勾选时
  的边框在 Chrome 里由 \`color-scheme\` 决定、不受作者 CSS 的 \`color\` 影响，这一点也记进了样式注释。

## [0.1.5] - 2026-09-17

### 新功能

- **Markdown 全屏预览与画布操作**：预览面板右上角的全屏按钮只给图表条目——通篇一张图的条目
  （如粘贴进来的 mermaid 源码）才需要放大看细节，长文档在 360px 的预览框里原生滚动反而更顺手，
  所以不给它们入口。全屏后内容成为画布，单指拖动平移、双指捏合缩放，桌面端为按住拖动平移，
  滚轮即缩放（以光标所在点为锚点）。底部「− / 百分比 / +」工具条点百分比即复位，缩放范围
  50%–400%，图表细节能放大看清、宽表格不必横向滚动。退出方式为右上角按钮或 \`Esc\`；
  有按钮的条目正文右侧留出通道，按钮不随内容滚走。
- **图片大图查看支持缩放与拖动**：列表点图弹出的灯箱里可滚轮/双指缩放、按住拖动平移，点空白处关闭；
  拖动松手不再被误判成一次点击而把灯箱关掉；切换图片自动回到 100%。
- **详情页内容图片可放大查看**：图片条目的内容图此前贴在左边、也点不开，现在水平居中，点击即打开
  与列表页同一套大图查看（滚轮/双指缩放、拖动平移，点空白或 \`Esc\` 关闭），不必回列表页才能看细节。
- **加密分组的解锁状态图标**：侧栏分组行与详情页「切换分组」下拉里，加密分组挂上锁图标，并用
  **敞口锁梁（已解锁）/ 闭合锁梁（未解锁）**区分状态，一眼能看出哪些分组带密码、当前解没解锁。

### 改进

- **解锁凭证落库**：分组密码、设置页密码、整站访问密码的解锁凭证原先只在内存里，服务一重启
  （升级应用、NAS 重启、崩溃拉起）就全部失效；而前端为了「刷新页面不用重新输密码」把它们存在
  浏览器本地，生命周期跨过了重启。两边对不上的表现是：界面亮着开锁图标，一操作就 401。现在凭证
  随签发写入数据库、启动时加载回内存，重启与升级后都不必重新解锁；48 小时有效期不变，到期仍需
  重新输入。升级到本版本后要重新解锁一次——旧的内存凭证不在库里。

### 修复

- **单行标题等结构文本不被识别为 Markdown**：\`# 这是一个标题\` 这类只有一行标题的记事此前被判成普通文本
  （单行列表 \`- 买牛奶\`、单行引用 \`> 不加班\` 同样漏判），因此不会按 Markdown 渲染。原因是判据要求
  「命中两种以上弱特征」才认定为文档，而单行文本最多只能命中一种。现在补上一条判据：**非空行里
  块级结构行不少于普通行**即认定为排版文档（围栏不计，代码片段仍判代码；含代码特征的片段如
  \`# 注释\` + \`func main() {}\` 仍判代码）。改动已随启动自动重算存量条目的类型。
- **CRLF 换行的表格不被识别**：从 Windows 复制来的表格分隔行行尾带 \`\\r\`，卡在行尾断言之前导致整张表
  判不出 Markdown。
- **英文散文被判成代码**：代码关键字此前做全串包含匹配，\`The class of 2020 was unusually large.\`、
  \`Please import data from the archive.\` 这类句子会带上代码图标。先改为要求关键字出现在行首且区分
  大小写；但行首恰好是小写关键字的句子（\`import data from the archive\`）仍会被误判，所以再补一层
  行形状判定：命中关键字的行含代码标点（括号、等号、引号、分号……）即认定为语句，没有标点则看它
  是不是一句自然语言（英文功能词 + 三个词以上）。\`import os\`、\`import numpy as np\`、\`package main\`
  这类只有两三个词、也没有功能词的语句不受影响，仍判代码。
- **改了内容但类型不跟着变**：条目类型是内容的派生值，但保存修改时前端会把打开时的旧类型一起提交，
  后端也只做「有就用、没有就保留」，于是把一条 \`# 标题\` 改成手机号后，它仍挂着 Markdown 的图标并按
  Markdown 渲染，而且详情页没有把它改回文本的入口，等于卡死在那里。现在内容被改动时后端按新内容
  重新识别，前端只在用户真的点过「按 Markdown 渲染」纠正入口时才提交类型 —— 人工纠正照旧生效。
- **带横线的手机号不被识别**：\`138-0013-8000\` 此前判为文本，手机号只认空格分段。同时补充 400/800 客服号
  （\`400-123-4567\`）识别；座机改为必须带区号，不再把 7-8 位纯数字（验证码、订单号、金额）当号码。
- **Mermaid 图表节点文字丢失**：\`flowchart\`、\`class\`、\`state\`、\`er\`、\`mindmap\` 的节点文字此前会被
  剥光、只剩空矩形。根因是 mermaid 默认把标签渲染成 \`<foreignObject><div>…</div></foreignObject>\`，
  而二次净化用的 DOMPurify 把 \`foreignobject\` 连同内容一并删除；改为 \`htmlLabels: false\` 走原生
  \`<text>\` 后全部恢复，净化策略不变。
- **灯箱缩放的中心不在鼠标上**：灯箱里图片是居中摆放的，而缩放参照系此前取的是视口原点，等于假定
  图片贴着屏幕左上角，于是缩放时内容围着图片左上角外侧缩放、光标下的细节会跑掉。改为按图片自身的
  布局原点计算后，光标下的内容点严格不动（滚轮缩放、双指捏合都受益）。
- **移动到已解锁的加密分组却提示「分组未解锁」**：换分组时后端除校验条目当前所属分组外，还要用
  **目标**分组的解锁凭证过写入鉴权，而请求只带了来源分组的那一个（前端按 URL 里的 \`?group=\` 注入
  单个凭证），目标分组于是被判成未解锁，回 401 又被统一翻译成「分组未解锁」——哪怕它已经解锁，
  也永远移不进去。现在移动请求显式带上手上全部已解锁凭证，且调用方显式给出的凭证头优先于 URL 的
  兜底注入。

## [0.1.4] - 2026-09-15

### 新功能

- **Mermaid 图表渲染**：Markdown 里的 \` \`\`\`mermaid \` 代码块（时序图、流程图、状态图、时间线、
  甘特图等）在浏览器端渲染成 SVG 图表，并跟随深浅主题切换自动重绘；语法有误时该块回退为源码，
  不会整页报错。Mermaid 与各图表类型按需加载，只有页面上真的出现图表时才下载，语法高亮与图表
  引擎都不进首屏主包；渲染结果经 \`securityLevel: 'strict'\` 与 DOMPurify 双重净化后才插入页面。
- **「预览 / Markdown」滑动开关**：Markdown 内容的预览与源码切换改为两段式滑动开关，首页卡片
  预览头与详情页内容字段位置一致、行为一致。详情页原「纯文本 / Markdown / 预览 / 编辑」四按钮
  分段控件随之移除；自动识别为非 Markdown、但内容实际是 Markdown 的条目（如只有一行 \`# 标题\`
  的短语法示例），字段右侧保留「按 Markdown 渲染」入口用于手动纠正，纠正随「保存修改」提交。

### 改进

- **标题不再显示文件后缀**：条目标题统一剥离扩展名（\`报告.pdf\` → \`报告\`），覆盖首页卡片、详情页
  标题、公开分享页与分享管理列表。后缀需以字母开头，因此 \`ventoy-1.1.17-windows\` 这类版本号不会被
  误判截断；类型图标与下载文件名不受影响。

### 修复

- **下载文件名丢扩展名**：标题被改成不带后缀后，下载下来的文件也没有扩展名，系统无法按类型关联
  打开方式。现在下载名以存储路径上的真实扩展名为准补全，标题怎么改都不影响下载。
- **中文标题的下载名兜底乱码**：\`Content-Disposition\` 的 ASCII 兜底此前按 UTF-8 字节逐个塞进
  \`filename=\`，中文标题会显示成一串乱码。现在兜底名把非 ASCII 字符按字节替换为 \`_\`，真实文件名交给
  \`filename*=UTF-8''\` 传递，现代浏览器取到的仍是完整中文名。

## [0.1.3] - 2026-09-15

### 新功能

- **分享访问与下载统计**：分享管理列表与公开分享页显示「访问 N · 下载 M」。访问按访客去重，
  同一访客 24 小时内只计一次（网关形态下按网关转发的登录用户身份区分，端口直连形态按来源 IP）；
  下载只统计页面上显式点击的下载，页面内的图片预览与音视频播放不计入——它们与下载共用同一个文件
  接口，且播放器会按 Range 分片多次请求，不加区分会把下载量刷高。
- **分享访问日志**：分享管理列表的操作区新增「日志」入口，按时间倒序列出该分享的访问与下载流水，
  含来源 IP、网关登录用户与发生时间；每个分享保留最近 200 条，撤销分享时随分享一并清理。日志记录
  的是全量流水，与去重后的计数口径不同（同一访客反复访问会留下多条记录）。
- **顶栏当前页标题**：设置页标题从内容区上移到顶栏居中显示；位于设置页时，右上角入口变为「回到
  首页」的动效图标。
- **移动端设置入口**：一级入口（访问密码 / 分组管理 / 分享管理）右侧增加进入箭头动效图标，页面
  顶部补充「返回首页」按钮。

### 改进

- **品牌图标跟随主题**：顶栏与分享页的 logo 改为内联 SVG 并以 \`currentColor\` 着色，浅色模式用
  主色墨绿、深色模式自动切为亮青，替换掉此前与全站配色不符的固定品牌蓝；favicon 与飞牛应用图标
  （\`ICON.PNG\` / \`ICON_256.PNG\` / \`images/icon_*.png\`）同步更新为同一主色。
- 设置入口右侧的进入箭头默认以弱化色渲染，悬停时随所在入口的文字一起变为主色。
- **升级兼容**：分享统计新增的计数字段与日志表在服务启动时自动迁移，已有数据库无需手动处理。

### 修复

- 修复移动端设置入口被「卡片行在窄屏纵向堆叠」的通用规则连带影响，导致进入箭头换行到第二行、
  并被拉伸到整行宽度的问题；现在入口保持单行布局，箭头贴在右内边距处。

## [0.1.2] - 2026-09-14

### 新功能

- **文件类型图标**：新增 147 种扩展名的图标资源，条目卡片的标题行与元信息行类型图标改为按扩展名
  匹配（文件名后缀优先，其次 MIME 子类型，如 \`image/svg+xml\` → \`svg\`）；未收录的类型回退到原有
  语义图标。图标为单色剪影，用 CSS mask + \`currentColor\` 着色，跟随文字颜色自适应明暗主题。
- **侧栏标签计数**：标签行右侧显示该标签关联的未删除条目数（后端 \`/api/tags\` 新增 \`count\`
  字段，口径与分组/分类计数一致）；数量为 0 的标签自动隐藏，侧栏不再出现空标签行。
- **悬停动效图标**：侧栏分类、类型下拉与卡片操作按钮引入 motion 驱动的动效图标，鼠标进入整行
  即播放、移出恢复静止（图标被挂上 ref 后转为受控，统一由整行 hover 驱动）。

### 改进

- 文件类型图标在统一网关形态下补上运行时前缀 \`window.__CP_BASE__\`，与接口请求一致地从
  \`/app/crosspaste\` 前缀下加载；构建时被内联为 data URI 的小图标不做前缀拼接。
- 侧栏分类与标签支持「整行 hover」：行高亮与图标动效同步触发，交互反馈更统一。

### 修复

- 修复 \`web/build.sh\` 同步前端产物时整体删除 \`app/ui\`，导致平台入口文件 \`config\`（桌面入口定义）
  与入口图标 \`images/\` 一并丢失的问题；现在只重建产物部分并保留这两项，打包不再缺失桌面入口。

## [0.1.1] - 2026-09-14

### 新功能

- **接入 fnOS 统一网关**：桌面图标改为通过 \`/app/crosspaste\` 访问，复用系统域名与 NAS
  登录态，无需记忆和输入端口号。后端新增 Unix Socket（\`target/app.sock\`）监听并处理网关
  路径前缀，随应用启停自动创建与清理。
- **安装/配置向导支持自定义服务端口**：安装与「应用设置」向导中可填写服务端口
  （范围 1024–65535，默认 28531），桌面入口与服务监听均跟随该值。
- **向导支持设置页访问密码**：安装时可直接初始化访问密码（留空则不启用，长度 4–64 位，
  使用 argon2id 哈希存储，不明文落盘）；在配置向导中填写新密码即可重置。
- **保留独立端口直连**：局域网内的手机、其他电脑仍可通过 \`http://NAS_IP:端口/\` 直接访问，
  与桌面网关入口双栈并存。

### 改进

- 前端改为**同一份构建产物自适应两种访问形态**：网关形态下由后端注入运行时前缀
  \`window.__CP_BASE__\`，路由 basename、API 请求前缀、静态资源与图标引用自动跟随；
  端口直连形态行为与旧版完全一致。
- 安装向导移除从未被实际消费的「媒体存储目录」字段，避免误导；媒体存储固定使用应用数据
  目录 \`var/media\`，同时保留对旧版本已保存自定义路径的读取兼容。
- 生命周期脚本与平台约定对齐：\`cmd/main\` 注入 \`GATEWAY_SOCKET\`，停止时兜底清理 socket
  文件；配置向导提交后密码即时写入数据库，无需手动重启。

### 修复

- 修复删除带访问密码的分组时，因页面缺少错误处理函数导入而产生的运行时报错；现在会正确
  提示「该分组带访问密码，请先解锁后再删除」。

## [0.1.0] - 2026-09-04

首版发布。

- 文本、图片、视频、音频、文件统一收纳，自动识别文本类型。
- 分组（支持私密分组访问密码）、标签、收藏与检索。
- 对外只读分享链接，支持撤销与续期。
- 独立 TCP 端口访问，开箱即用。
`})})}),n.jsxs("div",{className:"about-actions",children:[n.jsx("a",{className:"about-github",href:"https://github.com/YUNXI-fi/Crosspaste",target:"_blank",rel:"noreferrer","data-tip":"GitHub 仓库","aria-label":"GitHub 仓库",onMouseEnter:()=>{var a;return(a=o.current)==null?void 0:a.startAnimation()},onMouseLeave:()=>{var a;return(a=o.current)==null?void 0:a.stopAnimation()},children:n.jsx(i,{ref:o,size:16})}),n.jsx("button",{className:"btn-primary",type:"button",autoFocus:!0,onClick:e,children:"关闭"})]})]})})}export{m as default};
